name: Mirror Sync

on:
  schedule:
    - cron: '0 * * * *' # every hour
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push mirror
        run: |
          git remote set-url --push origin https://x-access-token:${{ secrets.REPO_PAT }}@github.com/DumpstarrFire/Database.git
          git fetch --all
          git push --mirror
        env:
          GIT_TERMINAL_PROMPT: 0
